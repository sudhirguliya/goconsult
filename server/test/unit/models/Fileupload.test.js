"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Fileupload');
const Fixture = require('../../fixtures/FileuploadFixture');

describe('models:Fileupload', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
