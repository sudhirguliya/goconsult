"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/SubCategory');
const Fixture = require('../../fixtures/SubCategoryFixture');

describe('models:SubCategory', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
