"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Plan');
const Fixture = require('../../fixtures/PlanFixture');

describe('models:Plan', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
