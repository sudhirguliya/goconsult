"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Email_template');
const Fixture = require('../../fixtures/Email_templateFixture');

describe('models:Email_template', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
