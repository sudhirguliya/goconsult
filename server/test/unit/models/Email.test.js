"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Email');
const Fixture = require('../../fixtures/EmailFixture');

describe('models:Email', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
