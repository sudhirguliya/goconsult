"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/SubCategoryController');

describe('controllers:SubCategoryController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
