"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/Email_templateController');

describe('controllers:Email_templateController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
