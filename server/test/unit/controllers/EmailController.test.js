"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/EmailController');

describe('controllers:EmailController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
